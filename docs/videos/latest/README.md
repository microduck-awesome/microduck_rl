# 最新 SC0090 视频 · 2026-09-15

首页：[最新训练视频](../../index.html)。全部为预先录制的视频，访问 Pages 无需连接本地电脑。

- 行走 V2 / 起身 V3 均使用本轮最终 `model_5999.pt`。
- 11 段行走与转向各 8 秒；五种起身各 4 秒；自由探索完整 60 秒。
- H.264、25 fps，播放时长对应真实仿真时长；不剪去未成功动作。
- 与本地模拟器相同的 CPU MuJoCo、SC0090 BAM、12 V、80 rpm、50 Hz 策略和归一化 ONNX。
- 每个场景固定录制一次，使用标称参数，不做域随机化。不是随机化成功率测试或实机测量。
- 行走前后/侧向速度使用机器人朝向的水平坐标系，转向角速度使用机身 z 轴；取第 2–8 秒平均。
- 起身使用标准坐姿、俯卧、仰卧、左侧卧、右侧卧。站稳标准与模拟器一致，耗时包括 0.5 秒稳定确认。
- 自由探索每轮自动混合行走、扰动和五种姿态；视频明确标注姿态重置，之后由模型自行起身。
  采用序列编号 1、每段指令 4 秒、起身等待上限 12 秒。轨迹重置/扰动时间列在 JSON 中。

`gallery.json` 保存模型路径与 SHA-256、各视频 SHA-256、实际帧数与时长、行走实测结果、
起身时间和完整探索动作记录。脚本位于 `simulator/scripts/render_gallery.py`。

旧 checkpoint 3450 / 3200 的视频仍在[历史基线](../../baseline.html)，测试协议不同。
